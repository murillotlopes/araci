import { AppEnvironment } from '../app/core/config/app-environment';

export const environment = {
  name: 'production',
  production: true,
  apiBaseUrl: 'https://smart.pin',
} satisfies AppEnvironment;
